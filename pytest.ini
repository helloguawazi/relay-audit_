[pytest]
testpaths = tests
addopts = -q
# tmp_path is redirected into the repo because some environments sandbox the
# system temp directory, which makes fixture setup fail with WinError 5.
tmp_path_retention_policy = none
